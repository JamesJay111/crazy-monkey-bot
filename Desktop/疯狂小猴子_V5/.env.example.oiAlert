# ============================================
# OI Alert 新架构配置示例
# ============================================

# 启用新架构（设置为 true 启用，false 或不设置则使用旧架构）
USE_NEW_OI_ALERT_ORCHESTRATOR=true

# Dry-run 模式（测试时设置为 true，生产时设置为 false）
OI_ALERT_DRY_RUN=false

# 基础配置
OI_ALERT_POLL_INTERVAL_MS=600000        # 扫描间隔（10 分钟）
OI_ALERT_THRESHOLD_PERCENT=10          # 阈值（10%）
OI_ALERT_COOLDOWN_WINDOW_MS=7200000    # 事件去重窗口（2 小时）

# 扫描配置
OI_ALERT_SCAN_TOP_N=200                 # 扫描 Top 200 个币种
OI_ALERT_SCAN_GROUPS=major,meme,topOI   # 扫描组（逗号分隔）
OI_ALERT_USE_DYNAMIC_LIST=true          # 使用动态列表
OI_ALERT_CONCURRENCY=5                   # 扫描并发数

# Lark Webhook（统一推送）
LARK_WEBHOOK_UNIFIED=https://open.larksuite.com/open-apis/bot/v2/hook/...

# Twitter OAuth 1.0a（已绑定账户会自动检测，无需配置）
# 如需绑定新账户，参考 TWITTER_OAUTH1_BINDING_GUIDE.md
